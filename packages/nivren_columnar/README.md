# nivren_columnar

A typed immutable columnar table foundation for integer, floating-point, and text data. Construction enforces unique names, equal column lengths, 4096-column and one-million-row ceilings; selection preserves types without converting values through strings.
