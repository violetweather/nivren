# nivren_svg

A bounded declarative vector-interface layer that renders deterministic SVG for browsers, desktop web views, documents, dashboards, and tests. Text and attributes are escaped, canvas dimensions and element counts are capped, and rendering needs no ambient authority.
