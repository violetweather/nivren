# nivren_wav

Bounded canonical RIFF/WAVE signed PCM16 encoding and decoding for audio generation, analysis, interoperability, and media pipelines. Sample ranges, frame alignment, header consistency, and a 16 MiB payload ceiling are enforced before data is returned.
