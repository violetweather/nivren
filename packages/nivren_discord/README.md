# nivren_discord

A capability-visible Discord REST foundation. It builds message JSON through typed shapes, constructs bot headers without logging tokens, and sends bounded certificate-verified requests through `std.web`.
