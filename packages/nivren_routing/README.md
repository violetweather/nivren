# nivren_routing

Deterministic exact-route values and lookup for small HTTP services. Matching is deliberately separate from socket handling, so routing remains pure, testable, and reusable with `std.web`.
