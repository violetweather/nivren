# nivren_image

Bounded RGB raster images with canonical binary PPM (P6) encoding and strict decoding. Dimensions, pixel counts, headers, comments, numeric fields, and the 16 MiB payload ceiling are checked before an image is returned.
