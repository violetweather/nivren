define greet(name: String) gives String {
    give "Hello, " + name + "!"
}

keep language: String = "Nivren"
show(greet(language))

change sum = 0
change number = 1
repeat (number <= 10) {
    sum = sum + number
    number = number + 1
}
show("The sum from 1 through 10 is:")
show(sum)
