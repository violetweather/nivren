keep language is String set "Nivren"
keep values is [Int] set [2, 3, 5, 7]

define add
takes {
    total is Int
    value is Int
}
gives Int
{
    give total + value
}

show("Hello, " + language + "!")
show(values through std.list.fold with { initial set 0 combine set add })
