shape CopyPlan holds {
    source is String
    destination is String
} derives Display, Validate, Arguments

define copy_configuration
takes {
    plan is CopyPlan
}
gives Nothing or String
needs FileRead
{
    keep contents set perform std.files.read with {
        path set plan.source
    } or give
    give ok(none)
}

prepare copy as CopyPlan with {
    source set "config/app.json"
    destination set "build/app.json"
}

perform copy_configuration with {
    plan set perform copy
}
